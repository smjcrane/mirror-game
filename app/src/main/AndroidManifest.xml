<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.ac.cam.sc989.mirrors">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityLevelSelect"
            android:label="@string/title_activity_level_select"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActivityPlayLevel"
            android:label="@string/title_activity_play_level"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ActivityTutorial"></activity>
    </application>

</manifest>